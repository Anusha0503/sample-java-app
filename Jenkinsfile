node {

     stage('checkout'){
       sh " echo 'git checkout done' "
     } 
     stage('build'){
      sh " echo ' build is running' " 
     }
     stage('deploy'){
       sh " echo 'deploy done' "
     }
}
